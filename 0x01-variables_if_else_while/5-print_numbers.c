#include <stdio.h>

/**
* main-prit snigle digite number
* followed by a new line
* Return: always 0 (Success)
*/
int main(void)
{
	int a;

	for (a = 0 ; a <= 9; a++)
	{
		printf("%d", a);
	}
	printf("\n");
	return (0);
}
