#include <stdio.h>

/**
* rev_string-function that revers string
* Return: Always 0
*/
void rev_string(char *s)
{
	int a = 0, b, c;
	char *c;

	while (s[a] != '\0')
	{
		a++;

	}
	c = a
	for (b = 0; a > 0;a--;b++)
	{
		s[a] = c[b];
	}
	for (b = 0; b < c;b++)
	{
		c[b] = s[b];
	}
}
